#include<string.h>

/**
 * *_strcmp - Prints +ve number if s1>s2, -ve if s1<s2, and 0 otherwise
 * @s1: first charcter string parameter
 * @s2: second character string parameter
 * Return: Always (Success)
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
