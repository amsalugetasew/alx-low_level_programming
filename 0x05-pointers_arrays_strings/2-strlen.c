#include "main.h"
#include <string.h>

/**
 * _strlen - Prints the length of string
 * @s: integer parameter
 * Return: Always (Success)
 */

int _strlen(char *s)
{
	return (strlen(s));
}

