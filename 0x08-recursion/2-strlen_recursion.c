#include "main.h"
#include <string.h>
/**
 * _strlen_recursion - Prints length string using recursion
 * @s: - first character parameter
 * Return: Always (Success)
 */
int _strlen_recursion(char *s)
{
	return (strlen(s));
}
