#include <stdio.h>
#include <stdlib.h>

/**
 * _abs - Prints absolute value of a number
 * @n: - integer parameter
 * Return: Always (Success)
 *
 */

int _abs(int n)
{
	int num;

	num = abs(n);
	return (num);
}
