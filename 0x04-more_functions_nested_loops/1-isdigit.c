
/**
 * _isdigit - Prints digits separtared by comma and sapace
 * @x: integer parameter
 * Return: Always (Success)
 */


int _isdigit(int x)
{
	if (x >= 48 && x <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
