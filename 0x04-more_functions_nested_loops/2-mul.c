#include <stdio.h>

/**
 * mul - return multiplication of two numbers
 * @a: first integer parameter
 * @b: second integer parameter
 * Return: Always 0.
 */
int mul(int a, int b)
{
	return (a * b);
}
